#include <stdio.h> // I/O 
#include <stdlib.h> // Library Standard for function like free malloc srand and rand

// Functions that need to be here for craps.c 

int main(void);
int saldo_inicial(void);
int apuesta(void);
int lanzadado(void);